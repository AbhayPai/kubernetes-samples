apiVersion: v1
kind: Service
metadata:
  name: mongoexpress-service
  namespace: mongo
spec:
  selector:
    app: mongoexpress
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
